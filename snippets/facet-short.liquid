<label for="SortBy-{{ context }}" class="text-nowrap {{ label_class }}">
  {{- 'products.facets.sort_by_label' | t -}}
  :</label
>
<div class="select w-auto facet-short flex items-center gap-2 flex-grow">
  {%- assign sort_by = results.sort_by | default: results.default_sort_by -%}
  <select
    name="sort_by"
    class="form-control form-control--select"
    id="SortBy-{{ context }}"
    aria-describedby="a11y-refresh-page-message"
    is="facet-short"
  >
    {%- for option in results.sort_options -%}
      {%- liquid
        if option.value == 'relevance'
          assign sort_label = 'shopify.search.sorting.relevance' | t
        else
          capture sort_t_key
            echo 'shopify.collections.sorting.'
            echo option.value | replace: '-', '_'
          endcapture
          assign sort_label = sort_t_key | t
          if sort_label == sort_t_key
            assign sort_label = option.name
          endif
        endif
      -%}
      <option
        value="{{ option.value | escape }}"
        {% if option.value == sort_by %}
          selected="selected"
        {% endif %}
      >
        {{ sort_label | escape }}
      </option>
    {%- endfor -%}
  </select>
  {% render 'icon-caret-down', size: '2xs' %}
</div>
